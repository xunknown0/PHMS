<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>PHMS System Error</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
<link rel="stylesheet" href="/stylesheets/errorstyle.css?v=<%= Date.now() %>">
</head>
<body>

  <!-- Animated Paw Prints -->
  <i class="paw-print paw-1 fas fa-paw"></i>
  <i class="paw-print paw-2 fas fa-paw"></i>
  <i class="paw-print paw-3 fas fa-paw"></i>

  <div class="error-container">
    <div class="error-header"><%= status || '500' %></div>

    <div class="error-card">
      <h2>
        <i class="fas fa-dog" style="color: var(--primary-green);"></i>
        Woof! Something went wrong.
      </h2>

      <p>
        We apologize! <strong>The Pet House Management System</strong> encountered an unexpected issue and cannot process your request.
      </p>

      <div class="details-section">
        <strong>Code:</strong> <%= status || '500' %><br>
        <strong>Message:</strong> <%= message || 'Something went wrong!' %>
      </div>

      <div class="text-center">
        <a href="/dashboard" class="btn-phms">
          <i class="fas fa-house"></i> Go Back Home
        </a>
      </div>
    </div>
  </div>

</body>
</html>
