     <div class="small">
                  <div class="d-flex align-items-center mb-1">
                    <i class="fas fa-envelope me-2 text-success-emphasis" style="width: 15px;"></i> 
                    <span class="text-dark"><%= o.email || 'N/A' %></span>
                  </div>
                  <div class="d-flex align-items-center mb-1">
                    <i class="fas fa-phone me-2 text-success-emphasis" style="width: 15px;"></i> 
                    <span class="text-dark"><%= o.phone || 'N/A' %></span>
                  </div>
                  <% if (o.phone2) { %>
                    <div class="d-flex align-items-center mb-1 text-muted">
                      <i class="fas fa-mobile-alt me-2" style="width: 15px;"></i> 
                      <span><%= o.phone2 %> (Secondary)</span>
                    </div>
                  <% } %>
                  
                  <div class="d-flex align-items-start mb-2 text-muted">
                    <i class="fas fa-map-marker-alt me-2 pt-1" style="width: 15px;"></i> 
                    <span class="flex-grow-1"><%= o.address || 'Address not listed' %></span>
                  </div>
                    <div class="d-flex align-items-center mt-2">
                  <i class="fas fa-paw me-2 text-success" style="width: 15px;"></i>
                  <strong class="text-dark">
                    <%= (o.petsCount || (o.pets ? o.pets.length : 0)) %>
                  </strong>
                  <span class="text-muted ms-1">
                    Pet<%= (o.petsCount || (o.pets ? o.pets.length : 0)) !== 1 ? 's' : '' %>
                  </span>
                </div>
                  </div>